import type { CollectionConfig } from 'payload'
export const Paintcolor: CollectionConfig = {
  slug: 'Paintcolor',

  fields: [
    {
      name: 'paintColor',
      type: 'text',
      required: true,
    }

  ],
}
